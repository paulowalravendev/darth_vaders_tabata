import 'package:flutter/material.dart';

abstract class AppColors {
  static const defaultBackgroundColors = Color(0xFF17191C);
  static const defaultTextColors = Color(0xFFFFFFFF);
  static const defaultButtonBackground = Color(0xFF603CA2);
}
